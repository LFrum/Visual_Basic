Module AddressBookModule
    ' g_addressCollection holds the Address objects.
    Public g_addressCollection As Collection

    ' g_itemToEdit is used by the AddForm form. It holds
    ' the name (key value) of the item in the collection
    ' that is to be edited.
    Public g_itemToEdit As String
End Module
